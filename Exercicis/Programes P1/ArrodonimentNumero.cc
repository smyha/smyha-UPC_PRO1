#include <iostream>
#include <cmath>
using namespace std;
int main(){

/*
    double x;
    cin >> x;
    int a = floor(x);
    int b = ceil(x);
    int c = round(x);

    cout << a << " " << b << " " << c << endl;