#include<iostream>
#include<vector>
using namespace std;

void read_data(vector<int>& v){
    int size = v.size();
    for (int i = 0; i < size; ++i){
        cin >> v[i];
    }
}
// Precondition: 0 ≤ p < v.size()
// Postcondition:
string sortida(int p, vector<int>& v){
    if
}

int main(){
    int p;
    cin >> p;

    vector<int> v(p);

}
